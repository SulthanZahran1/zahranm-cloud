# AGENTS.md

## Agent skills

### Issue tracker

Work is tracked on this repo's GitHub Issues — the wayfinder map issue (label `wayfinder:map`) and its child decision tickets (`wayfinder:research`, `wayfinder:prototype`, `wayfinder:grilling`, `wayfinder:task`). Tickets are child issues of the map with native blocking. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` (glossary, implementation-free) + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
